{
  "name": "epoche-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "astro": "^4.6.0",
    "wrangler": "^4.87.0"
  },
  "version": "1.1.0",
  "dependencies": {
    "@astrojs/cloudflare": "11"
  }
}
